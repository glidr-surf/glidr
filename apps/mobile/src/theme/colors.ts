import { colors as tokenColors } from '@glidr/tokens';

export const colors = {
  ...tokenColors,
  cardDark: '#2A2720',
  white: '#FFFFFF',
  black: '#000000',
  overlay: 'rgba(26,23,20,0.7)',
} as const;
