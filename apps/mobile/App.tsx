import { colors } from '@glidr/tokens';

// Placeholder — proves @glidr/tokens is wired up in the monorepo.
// The mobile app will be built in a separate brief.
console.log('Glidr mobile placeholder. bg color:', colors.bg);
